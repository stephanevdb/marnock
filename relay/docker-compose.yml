services:
  relay:
    image: ghcr.io/stephanevdb/marnock-relay:latest
    container_name: marnock-relay
    ports:
      - "${RELAY_PORT:-8787}:8787"
    restart: unless-stopped
    pull_policy: always
