<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Marnock" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/marnock_teal</item>
        <item name="colorSecondary">@color/marnock_ink</item>
        <item name="android:colorBackground">@color/marnock_mist</item>
    </style>

    <!-- Opaque + focusable: translucent themes often never receive window focus on OEM builds. -->
    <style name="Theme.Marnock.ClipboardCapture" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowDisablePreview">true</item>
    </style>
</resources>
